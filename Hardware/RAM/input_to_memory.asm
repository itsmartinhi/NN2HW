.org 0x000 ; start at the "first" adress
.start

mov bx 1111b ; the b is important to tell the compiler that this is a binary value

; mov could be the one, st (store) could also be the one, every
; command seems to work with registers tho and the it can't be
; written into the memory directly